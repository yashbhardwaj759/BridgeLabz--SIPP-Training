// Superclass Person
class Person {
    private String name;
    private int age;

    public Person(String name, int age) {
        this.name = name;
        this.age = age;
    }

    // Getters
    public String getName() {
        return name;
    }

    public int getAge() {
        return age;
    }

    public String displayRole() {
        return "Person: " + name + ", Age: " + age;
    }
}

// Subclass Teacher
class Teacher extends Person {
    private String subject;

    public Teacher(String name, int age, String subject) {
        super(name, age);
        this.subject = subject;
    }

    @Override
    public String displayRole() {
        return "Teacher: " + getName() + ", Age: " + getAge() + ", Subject: " + subject;
    }
}

// Subclass Student
class Student extends Person {
    private int grade;

    public Student(String name, int age, int grade) {
        super(name, age);
        this.grade = grade;
    }

    @Override
    public String displayRole() {
        return "Student: " + getName() + ", Age: " + getAge() + ", Grade: " + grade;
    }
}

// Subclass Staff
class Staff extends Person {
    private String department;

    public Staff(String name, int age, String department) {
        super(name, age);
        this.department = department;
    }

    @Override
    public String displayRole() {
        return "Staff: " + getName() + ", Age: " + getAge() + ", Department: " + department;
    }

    public static void main(String[] args) {
        Teacher teacher = new Teacher("Alice Smith", 35, "Mathematics");
        Student student = new Student("Bob Johnson", 15, 10);
        Staff staff = new Staff("Carol White", 40, "Administration");
        System.out.println(teacher.displayRole());
        System infestedStudent.displayRole());
        System.out.println(staff.displayRole());
    }
}